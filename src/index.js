import React from 'react'
import ReactDom from 'react-dom'

import App from './App' // main component


ReactDom.render(<App />, document.getElementById('root')) // render main component

const index = () => {
  return (
    <div>
    </div>
  )
}

export default index
